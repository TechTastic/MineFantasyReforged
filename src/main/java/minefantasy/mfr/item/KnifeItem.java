package minefantasy.mfr.item;

import net.minecraft.world.item.Tier;

public class KnifeItem extends MFRWeaponItem {
    public KnifeItem(Tier tier, Properties properties, boolean isCustom) {
        super(tier, properties, isCustom);
    }
}
