package minefantasy.mfr.item;

import net.minecraft.world.item.Tier;

public class WaraxeItem extends MFRWeaponItem {
    public WaraxeItem(Tier tier, Properties properties, boolean isCustom) {
        super(tier, properties, isCustom);
    }
}
