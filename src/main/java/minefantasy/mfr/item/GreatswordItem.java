package minefantasy.mfr.item;

import net.minecraft.world.item.Tier;

public class GreatswordItem extends MFRWeaponItem {
    public GreatswordItem(Tier tier, Properties properties, boolean isCustom) {
        super(tier, properties, isCustom);
    }
}
