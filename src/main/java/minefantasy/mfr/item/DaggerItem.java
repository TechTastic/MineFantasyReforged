package minefantasy.mfr.item;

import net.minecraft.world.item.Tier;

public class DaggerItem extends MFRWeaponItem {
    public DaggerItem(Tier tier, Properties properties, boolean isCustom) {
        super(tier, properties, isCustom);
    }
}
