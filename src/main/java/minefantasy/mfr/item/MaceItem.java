package minefantasy.mfr.item;

import net.minecraft.world.item.Tier;

public class MaceItem extends MFRWeaponItem {
    public MaceItem(Tier tier, Properties properties, boolean isCustom) {
        super(tier, properties, isCustom);
    }
}
